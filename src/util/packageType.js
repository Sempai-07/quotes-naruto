const packageType = (separator = ', ') => {
  let content = `description${separator}clan${separator}status${separator}date${separator}growth${separator}weight${separator}element${separator}rank${separator}family`
};

module.exports = packageType;